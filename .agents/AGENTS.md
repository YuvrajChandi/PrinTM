## Documentation Integrity Rule
- Always update `backend_api_spec.md` and `backend_db_spec.md` automatically whenever changes are made to API contracts (endpoints, methods, payloads) or the database schema (tables, fields, relations) without requiring the user to explicitly request it.
- Do not push to GitHub unless explicitly requested by the user.
